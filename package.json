{
  "name": "coral-fs-tasks",
  "version": "1.0.1",
  "description": "Modules that wraps a few functions of Node's 'fs' module inside Tasks (from folktale/data.task), for easy function composition (using, for example, Ramda's composeK) ",
  "main": "fs-tasks.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --colors --reporter spec ./**/*spec.js"
  },
  "author": "Diego Villaseñor",
  "license": "ISC",
  "devDependencies": {
    "cheerio": "^0.22.0",
    "gulp": "^3.9.1",
    "gulp-mocha": "^4.3.1",
    "gulp-watch": "^4.3.11",
    "jasmine-node": "^1.14.5",
    "proxyquire": "^1.7.10"
  },
  "dependencies": {
    "data.task": "^3.1.1",
    "ramda": "^0.23.0",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "request": "^2.79.0"
  }
}
